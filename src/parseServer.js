/* Simple file to parse the tree into a useful structure. */

function parseNewickTree() {
    // This function will parse a newick tree using newick.js and iterate through the resulting object to create
    // the phylotree structure
    
    
}
